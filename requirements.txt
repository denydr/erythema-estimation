numpy
scipy
h5py
pandas
Pillow
matplotlib
python-dotenv
mediapipe==0.10.35
# Stage 3 U-Net model (segmentation_models_pytorch pulls in timm for encoders).
# Pin exact versions after the first successful install (see reproducibility note in README).
torch==2.13.0
torchvision==0.28.0
segmentation-models-pytorch==0.5.0
scikit-image==0.26.0  # SSIM for evaluation (scripts/evaluate.py)
jupyter>=1.0
